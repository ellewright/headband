import { Typography } from "@mui/material";

export default function Add() {
    return (
        <Typography
            sx={{
                marginTop: "4.5rem"
            }}
        >
            Add
        </Typography>
    );
};