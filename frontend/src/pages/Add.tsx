import { Container, Typography } from "@mui/material";

export default function Add() {
    return (
        <Container>
            <Typography
                sx={{
                    marginTop: "5.5rem"
                }}
            >
                Add
            </Typography>
        </Container>
    );
};