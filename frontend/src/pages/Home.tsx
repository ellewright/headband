import { Container, Grid2, Typography } from "@mui/material";

export default function Home() {
    return (
        <Container>
            <Typography
                sx={{
                    marginTop: "5.5rem"
                }}
            >
                Home
            </Typography>
        </Container>
    );
};